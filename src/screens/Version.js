import React, {useEffect} from 'react';
import {
  StyleSheet,
  Text,
  View,
  Image,
  TouchableOpacity,
} from 'react-native';
import '../utils/global';


const VersionScreen=()=>{
    return (
        <View style={styles.container}>
          <Text style={styles.sublight}>Login</Text>
        
        </View>
        );
}

const styles = StyleSheet.create({});
export default VersionScreen;